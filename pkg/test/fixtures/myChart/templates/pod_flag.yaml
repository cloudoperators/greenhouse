{{ if .Values.enabled }}
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: alpine
  name: alpine-flag
spec:
  containers:
  - image: alpine:{{.Values.imageTag}}
    name: alpine
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
{{ end }}
